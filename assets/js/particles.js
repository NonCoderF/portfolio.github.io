(() => { const canvas = document.getElementById('particles'); if (!canvas || matchMedia('(prefers-reduced-motion: reduce)').matches) return; const ctx = canvas.getContext('2d'), dots = []; let w, h, frame; const resize = () => { w = canvas.width = innerWidth * devicePixelRatio; h = canvas.height = canvas.offsetHeight * devicePixelRatio; ctx.scale(devicePixelRatio, devicePixelRatio); w /= devicePixelRatio; h /= devicePixelRatio; }; const init = () => { dots.length = 0; for (let i = 0; i < Math.min(55, innerWidth / 20); i++) dots.push({ x: Math.random() * innerWidth, y: Math.random() * innerHeight, vx: (Math.random() - .5) * .15, vy: (Math.random() - .5) * .15, r: Math.random() * 1.4 + .3 }); }; const draw = () => { ctx.clearRect(0, 0, innerWidth, innerHeight); dots.forEach((p, i) => { p.x += p.vx; p.y += p.vy; if (p.x < 0 || p.x > innerWidth) p.vx *= -1; if (p.y < 0 || p.y > innerHeight) p.vy *= -1; ctx.fillStyle = i % 4 ? 'rgba(183,243,107,.28)' : 'rgba(133,167,255,.35)'; ctx.beginPath(); ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2); ctx.fill(); }); frame = requestAnimationFrame(draw); }; addEventListener('resize', () => { cancelAnimationFrame(frame); resize(); init(); draw(); }, { passive: true }); resize(); init(); draw(); })();
