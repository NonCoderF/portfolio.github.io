(() => { const canvas=document.getElementById('particles'); if(!canvas||matchMedia('(prefers-reduced-motion: reduce)').matches)return; const ctx=canvas.getContext('2d'); let w,h,dpr,raf; const dots=[]; const resize=()=>{dpr=Math.min(devicePixelRatio,2);w=innerWidth;h=canvas.offsetHeight;canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0);dots.length=0;for(let i=0;i<Math.min(75,w/16);i++)dots.push({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.16,vy:(Math.random()-.5)*.16,r:Math.random()*1.3+.2});}; const draw=()=>{ctx.clearRect(0,0,w,h);ctx.strokeStyle='rgba(0,245,255,.035)';ctx.lineWidth=1;const size=48;for(let x=0;x<w;x+=size){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,h);ctx.stroke()}for(let y=0;y<h;y+=size){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke()}dots.forEach((p,i)=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;ctx.fillStyle=i%5?'rgba(0,245,255,.3)':'rgba(0,255,163,.38)';ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill();if(i<18){ctx.strokeStyle='rgba(124,58,237,.09)';ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(p.x+p.vx*80,p.y+p.vy*80);ctx.stroke()}});raf=requestAnimationFrame(draw)};addEventListener('resize',()=>{cancelAnimationFrame(raf);resize();draw()},{passive:true});resize();draw();})();
